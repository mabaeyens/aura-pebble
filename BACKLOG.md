# Backlog

## Done

- 2026-09-04 **aura-essential: worldwide city search in the settings, and cut `essential-v1.2.0`.** Replaced the fixed 23-city dropdown (the `CITY` select plus its hard-coded `CITIES` coordinate table in `index.js`) with the same worldwide typeahead the analog/weather faces use: a `SEARCH` input whose keyup runs a 300 ms-debounced query against the keyless `geocoding-api.open-meteo.com`, rendering up to 8 matches; tapping one fills the `Selected` (`CITY`, display only), `LAT`/`LON`, and turns `USE_GPS` off. Location now resolves as: GPS on (default) uses the phone; GPS off uses the typed/picked lat-lon. Dropped the old `LOCMODE` toggle for `USE_GPS`, and added `SEARCH`/`USE_GPS` to `messageKeys` (needed a `pebble clean`; the C is unchanged since location lives entirely phone-side). The typeahead is wired inside `clayStyle` (self-contained, since Clay serialises it into the config page). Builds clean on all five platforms at versionLabel 1.2.0.
- 2026-09-03 Built **Aura Weather** v1, the whole `docs/00` build order in five committed steps (provider decision settled on Open-Meteo; units default to the phone locale; standalone PebbleKit JS, zero app changes). Step 1: scaffolded the watchapp (uuid `fce83b9e...`, `watchface=false`, emery-first, Weather Icons + Liberation Sans subset fonts) and drew the signature hero, the live sun (moon at night) arcing over a procedural day/night sky from `sky.c`, ported from `AuraSunPath` with the SDK trig table (no libm). Step 2: the keyless Open-Meteo bridge end to end, one call for current+hourly+daily+sun times (`timeformat=unixtime`), normalised to the 10-value code set, chained as one current + 8 hourly + 6 daily AppMessage frames, filled into a persisted `Weather` struct; SELECT refreshes. Step 3: the hourly (12/24h labels, night glyphs from sun times) and daily (weekday, min/max tinted by the shared ramp) screens, both bounds-relative, with a thin picton-blue precip bar at pop >= 20. Step 4: the Clay settings screen with a custom worldwide typeahead against `geocoding-api.open-meteo.com` (injected via `customFn`, self-contained), `geo.nearestINE` resolving a Spanish pick to its INE over the 737 KB trimmed `municipios.min.json` (phone-side only), units auto/C/F, GPS toggle, and the AEMET key fields. Step 5: the optional AEMET path, `fetchAEMET` porting the two-call envelope/`datos` model for the diaria and horaria endpoints, `shapeAEMET` returning the same normalised object (coarse daily/hourly blocks collapsed, `orto`/`ocaso` sun times, night suffix stripped, Celsius to Fahrenheit on the phone), routed to only when the location has an INE and the user enabled it with a key, falling back to Open-Meteo on any AEMET failure. Builds clean for all five platforms; hero, hourly and daily verified on the emery emulator with live data; the AEMET parser verified offline against a synthetic payload. Still needs a check on device: the in-page Clay typeahead (Clay DOM internals, not drivable headless) and the live AEMET two-call with a real key (the Open-Meteo fallback makes that safe). Not added to the top-level README or published yet; that is a release decision for later.
- 2026-09-02 Drafted the **Aura Weather** spec series in a new `aura-weather/` folder (README + `docs/00`..`05`; kept out of the gitignored `specs/` name so they track): a standalone Pebble Time 2 weather **watchapp** (not a face) porting Aura's design language. Key findings recorded: the source app is Spain-only (AEMET, INE-keyed, no geocoder, no worldwide provider), so worldwide search + a second provider are net-new work, not a port. The genuinely portable crown jewel is the sun-path maths (`AuraSunPath`/`SolarTimes`, pure NOAA arithmetic, zero assets); the ~280 baked hero images, SF Symbols, blur, observations/radar/CAP-warnings do not port. Specced: provider-agnostic phone-side JS bridge (Open-Meteo keyless worldwide, AEMET two-call for Spain), a 10-value normalised condition enum so the watch never sees a raw provider code, a chunked AppMessage protocol (current + 8 hourly + 6 daily frames chained on `outbox_sent`), a compact on-watch `Weather` struct persisted for instant launch, a custom Clay config with worldwide typeahead (`geocoding-api.open-meteo.com`) plus Spain-to-INE resolution over the 842 KB / 8122-entry `municipios.json` (bundled phone-side only), and the hero screen with the live sun/moon arcing over a procedural day/night sky. App UUID minted: `fce83b9e-37bc-4714-9125-840d83a72d64`. `watchapp.watchface=false`, capabilities `[configurable,location]` (no health). Companion question answered in `05`: all five must-haves work with **zero** iOS/Android changes via PebbleKit JS; making Aura the companion (PebbleKit iOS + Android, both, separately) buys only key-free AEMET for Spanish users. Recommendation: standalone JS for v1, companion as an optional Phase 2. **Open decision for the user: worldwide provider (A keyless Open-Meteo everywhere + AEMET optional [recommended], B AEMET-key Spain + keyless world, C AEMET + OpenWeather both keys). Specs are built toward A until decided.** Nothing is built yet; this is design only.
- 2026-09-02 Replaced the dashboard icons with the **real Aura brand icon** (the white A whose swish crosses from the lower-left leg into a wave on the right, over a blue gradient with a warm corner glow) instead of the earlier coral "A + underline" redraw. Committed the 1024px master at `docs/store/aura-brand-icon.png` (Aura's own asset, also at askmira.es/aura/assets/icon.png) and reworked `scripts/make-app-icons.sh` to just Lanczos-downscale it to 80x80 and 144x144. The icon is dashboard-only, so it needs no Pebble-specific redraw. NB the brand icon's warm glow sits top-left, not top-right.
- 2026-09-02 Created the first appstore/dashboard icons for Essential: the Aura "A + swish" mark, white on the classic face's coral (`#E66E6B`, sampled from `essential-classic.png`), at **80x80** (Small Icon) and **144x144** (Large Icon) under `docs/store/app-icon-80.png` / `app-icon-144.png`. Added `scripts/make-app-icons.sh` to regenerate them. (Superseded same day by the brand-icon version above.)
- 2026-09-02 Deleted the orphaned `docs/analog-light.png`, `docs/analog-dark.png` and `docs/emery-preview.png` (unreferenced since the README hero switched to Essential). Analog and digital get fresh builds + screenshots before they're published.
- 2026-09-02 **Published aura-essential 1.1.0 to the Pebble appstore** (apps.repebble.com). The CloudPebble build succeeded (once the `message_keys.auto.h` fix landed on the `cloudpebble-essential` Emery-only branch) and the face is now live at https://apps.repebble.com/aura-essential_19335d2e746b4d6aa6ba8c63. First Aura face on the store. Linked from the README (Faces table + under the hero).
- 2026-09-02 Built a looping GIF of the five Essential themes (`docs/store/essential-themes.gif`, 200x228, 5 frames, 1.2s each) and attached it to the `essential-v1.1.0` GitHub release.
- 2026-09-02 Cut the `essential-v1.1.0` GitHub release (Latest; 1.0.1 and 1.0.0 kept as history): clean all-five-platform `.pbw` verified at versionLabel 1.1.0, source zip, five theme screenshots, and the themes GIF.
- 2026-09-02 Fixed the CloudPebble build failure (`undefined reference to MESSAGE_KEY_*` at link). Root cause: CloudPebble runs pebble-tool **5.0.39**, which emits the message keys as `#define` macros with no globals to link, while my local **5.0.40** emits them as `extern uint32_t` globals defined in `message_keys.auto.c`. Both faces hand-declared the keys `extern`, which only links against 5.0.40. Switched both `aura-essential.c` and `aura-analog.c` to `#include "message_keys.auto.h"`, which works with either convention. Bumped essential 1.0.1 -> **1.1.0** and analog 1.2.0 -> **1.3.0** (minor, store-safe `major.minor.0`). Both build clean locally on all five platforms.
- 2026-09-02 Pinned the `cloudpebble-essential` deploy branch to **Emery only** for fast single-device CloudPebble iteration: `scripts/sync-cloudpebble.sh` now adds a commit on top of the subtree split that rewrites `targetPlatforms` to `["emery"]` (via a detached worktree), so it survives every re-sync. `main` keeps all five platforms for the distributable `.pbw` — do not publish the Emery-only CloudPebble build to the store.
- 2026-09-02 README hero row now shows all five Essential themes (added forest and sunset to classic/ocean/midnight); confirmed the store shots already reflect the polished face, so no regeneration was needed.
- 2026-09-02 CloudPebble monorepo workaround: a plain GitHub sync builds the first subdir it finds (`aura-analog`, alphabetically) because there is no project at the repo root. Added `scripts/sync-cloudpebble.sh <face>` (a `git subtree split` that force-pushes a `cloudpebble-<face>` deploy branch whose root is the face), created and pushed `cloudpebble-essential`, and documented this plus the zip-import alternative in `docs/DISTRIBUTION.md`.
- 2026-09-02 Menu icon: drew a 25x25 white-on-transparent "Aura A with swish" (`docs/store/menu-icon.png`) for the appstore listing, and bundled it as `IMG_MENU_ICON` (bitmap) in both aura-analog and aura-essential so CloudPebble's publish menu-image option can pick it up.
- 2026-09-02 Named **Kiezel** as the original Essential's developer in the README Design origin note, and rewrote the 1.0.1 GitHub release notes with a first-person inspiration section (Kiezel's Essential, on the appstore since 2015 and unmaintained since 2016, a Kickstarter-era favourite) and CloudPebble-first install steps.
- 2026-09-02 Released aura-essential 1.0.1 on GitHub (tag `essential-v1.0.1`): bumped `package.json` to 1.0.1, verified the manifest conforms to the watchface spec (`watchapp.watchface true`, `emery` in `targetPlatforms`, all five platforms bundled, `configurable`/`location`/`health` capabilities all used, Clay defaults match the C source), clean `pebble clean` + `pebble build`, and attached the fresh `.pbw`, a source zip and the five store screenshots. Rolls up the polish pass below.
- 2026-09-02 Essential polish pass: sun rays/outline uniform 3px; overcast (WMO 3) now a plain cloud with no sun; clouds redrawn with a scalloped, rounded bottom (row of bump discs) instead of a flat bar; weekday moved from Gothic to the bundled **LECO 1976 Regular** so it matches the LECO numbers; battery resized to sit level with the other icons and to read its fill clearly; the top-block icon + label group vertically centred (measured 25px above / 25px below).
- 2026-09-02 Regenerated all five store screenshots on the polished face, each calendar showing a distinct day + weekday (classic 9 Fri, ocean 18 Sun, midnight 11 Wed, forest 23 Mon, sunset 30 Thu) and a varied complication mix.
- 2026-09-02 Credited LECO 1976 Regular (© Samuel Čarnoký / CarnokyType) and its free MyFonts Desktop + App license in the README Fonts section; confirmed the font is free for both the embedded `.pbw` and the store screenshots.
- 2026-09-02 Released aura-essential 1.0.0 on GitHub (tag `essential-v1.0.0`): clean production `.pbw`, a fresh source zip, and the five store screenshots as assets.
- 2026-09-02 README hero row now features the Essential face in three themes (classic, ocean, midnight), replacing the analog Mondaine preview images.
- 2026-09-02 Partly-cloudy weather icon: sun now drawn on top of the cloud and fully visible, with a uniform 3px black outline all the way around (same disc as the outer halo, skipped in the black halo passes so it does not compound).
- 2026-09-02 Calendar date vertically centred in the white body by measuring the LECO glyph (`graphics_text_layout_get_content_size`, 5-arg form) and placing it at `cy+3`, instead of top-anchoring the box which bottom-aligned the digits.
- 2026-09-02 Added five store screenshots under `docs/store/` (classic, ocean, forest, midnight, sunset), each a different block-colour theme and complication mix.
- 2026-09-01 aura-essential built: three-block Essential replica with three configurable complications, LECO time and numeric labels, bitmap shoe icon, Clay settings, and the Open-Meteo weather bridge.

## Pending

- Aura Weather, two on-device checks the headless emulator could not cover. (1) The in-page Clay city typeahead exercises Clay's DOM internals (`getItemByMessageKey(...).$manipulatorTarget`, `.set()`) in the config browser, which I can't drive here: open settings, search a city, confirm result rows appear and tapping one fills city/lat/lon and flips GPS off. Manual lat/lon and GPS are already verified. (2) The live AEMET two-call with a real key (the parser is verified offline against a synthetic payload; the Open-Meteo fallback makes a live bug non-fatal). Sideload with `pebble install --cloudpebble`.
- Aura Weather is not in the top-level README table and not published. Adding it there and shipping to the appstore is a later release decision.
- Publish aura-analog and aura-digital when each has a proper production build and its own store screenshots. Analog is bumped to 1.3.0 in source but has no GitHub release yet; digital has neither. Neither is close to release. Essential is the current focus.

## Notes

- Dashboard/appstore listing icon = the **real Aura brand icon** downscaled, not a Pebble-specific redraw. Master at `docs/store/aura-brand-icon.png` (1024px, also served at askmira.es/aura/assets/icon.png); `scripts/make-app-icons.sh` Lanczos-downscales it to `app-icon-80.png` (Small) and `app-icon-144.png` (Large). Only used on the developer dashboard, so it does not need to match the Pebble launcher's tinting rules like `menu-icon.png` does. Don't redraw it.
- CloudPebble runs an older **pebble-tool (5.0.39)** than local (5.0.40), and the two disagree on message-key codegen: 5.0.40 emits `MESSAGE_KEY_*` as `extern uint32_t` globals defined in `message_keys.auto.c`; 5.0.39 emits `#define` macros with no globals. Always `#include "message_keys.auto.h"` in C (works with both); never hand-declare the keys `extern` (links locally, fails on CloudPebble with `undefined reference`). This is what broke the first CloudPebble build of essential.
- Store screenshots are now generated **without patching the source**: build+install once, then drive each theme with `pebble send-app-message --emulator emery --app-uuid <uuid> --int <key>=<val> ...` using the runtime message-key IDs (SLOT1..3 = 10000..10002, TOPCOLOR/BANDCOLOR/BOTCOLOR = 10003..10005, SEPCOLOR 10007, COMPCOLOR 10008, TIMECOLOR 10009, WX_TEMP/WX_CODE/WX_OK = 10015..10017). Inject demo data with `emu-steps 8432`, `emu-battery --percent 100`, `emu-heart-rate 68`, and set the calendar day via `emu-set-time <unix>` (compute the epoch against Europe/Madrid so the clock reads 10:09 despite CET/CEST). Heart-rate gotcha: set the HR **before** the redraw-triggering `emu-set-time` (with a short sleep) or the bpm label renders empty. This leaves the tracked source completely untouched — no restore step.
- After any store-shot run, `aura-essential/build/aura-essential.pbw` still holds the last demo *settings* in emulator persist, but the tracked source is clean; rebuild with `pebble build` for a shippable `.pbw`.
- The emery emulator mutes saturated hues in its display simulation (canonical orange renders as coral). A real Time 2 shows it closer to the true `GColorOrange`. Not a bug.
- Publishing path: `pebble publish` uploads to the appstore dashboard API at `https://appstore-api.repebble.com` (override with `--api-base` or `PEBBLE_APPSTORE_API_BASE`), authenticated with a Firebase ID token from `pebble login`. `pebble install --cloudpebble`/`--phone` use the CloudPebble relay to sideload to a real watch. Login status was logged out at the end of this session.
- Menu image: bundled as `IMG_MENU_ICON` in each face (not referenced in C, ~0.5KB) so CloudPebble's publish flow can select it; the alternative is uploading `docs/store/menu-icon.png` directly on the developer dashboard. Drawn white-on-transparent so the launcher tints it.
- CloudPebble + this monorepo: CloudPebble builds the project at the repo **root**, and this repo has none (three faces in subdirs, `aura-analog` sorts first). Build a specific face via its `cloudpebble-<face>` deploy branch (regenerate with `scripts/sync-cloudpebble.sh <face>`; it force-pushes the subtree split, which is expected because the branch is a mirror, not shared work), or import `<face>-<version>-cloudpebble.zip` (project at the zip root, built with `git archive HEAD:<face>`). Those `*-cloudpebble.zip` import artifacts are gitignored.
- Appstore version format: `docs/DISTRIBUTION.md`'s checklist wants `version` = `major.minor.0`. The SDK built and released `1.0.1` cleanly and it is fine as a GitHub release, but the Pebble store historically reads only major.minor, so confirm the live portal accepts a non-zero patch before publishing, else bump to `1.1.0`.
