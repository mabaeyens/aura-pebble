# Pebble build output (one build/ per face project)
build/
*.pbw

# CloudPebble import zips (regenerated on demand from a face subdir)
*-cloudpebble.zip

# Pebble tooling
.pebble-cache/
node_modules/
.lock-waf_*

# Local specs (per Projects convention — specs are local-only)
specs/

# macOS
.DS_Store

# Obsidian vault config (local editor state)
.obsidian/
